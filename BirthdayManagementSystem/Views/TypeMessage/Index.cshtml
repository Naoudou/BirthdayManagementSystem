@model IEnumerable<BirthdayManagementSystem.Models.TypeMessage>

@{
    ViewBag.Title = "Index";
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Label)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Label)
        </td>
        <td>
            @Html.ActionLink("Modif", "Edit", new { id=item.IdTypeMessage }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTypeMessage }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.IdTypeMessage })
        </td>
    </tr>
}

</table>
